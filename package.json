{
  "name": "game-shooter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc src/server/server.ts --outDir dist/server",
    "start": "node dist/server/server.js",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
